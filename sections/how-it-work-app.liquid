<style>
  #shopify-section-{{ section.id }} .how_it_work_app {
    --accent-color: {{ section.settings.accent_color }};
    background: #000000;
    color: #ffffff;
    overflow: hidden;
    padding: {{ section.settings.desktop_padding_top }}px 0 {{ section.settings.desktop_padding_bottom }}px;
  }

  #shopify-section-{{ section.id }} .how_it_work_app * {
    box-sizing: border-box;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .inner {
    margin: 0 auto;
    max-width: 1440px;
    padding: 0 24px;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .header {
    display: block;
    justify-content: start;
    margin: 0 0 38px;
    max-width: none;
    padding: 0;
    text-align: left;
    width: 100%;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .eyebrow {
    color: var(--accent-color);
    font-size: 18px;
    font-weight: 600;
    line-height: 1.25;
    margin: 0 0 14px;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .section_title {
    color: #ffffff;
    font-size: clamp(32px, 3vw, 48px);
    font-weight: 500;
    letter-spacing: -0.035em;
    line-height: 1.05;
    margin: 0;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .section_description {
    color: #ffffff;
    font-size: 18px;
    line-height: 1.35;
    margin-top: 18px;
    max-width: 620px;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .section_description p,
  #shopify-section-{{ section.id }} .how_it_work_app .slide_description p {
    margin: 0;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .carousel {
    margin: 0 auto;
    max-width: 1100px;
    position: relative;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .viewport {
    margin: 0 auto;
    max-width: 760px;
    overflow: hidden;
    touch-action: pan-y;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .track {
    align-items: flex-start;
    display: flex;
    transition: transform 0.45s cubic-bezier(0.22, 1, 0.36, 1);
  }

  #shopify-section-{{ section.id }} .how_it_work_app .slide {
    flex: 0 0 100%;
    min-width: 0;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .image_wrap {
    aspect-ratio: 3 / 4;
    background: #282828;
    border-radius: 12px;
    margin: 0 auto;
    max-width: 460px;
    overflow: hidden;
    position: relative;
    width: 100%;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .image,
  #shopify-section-{{ section.id }} .how_it_work_app .video,
  #shopify-section-{{ section.id }} .how_it_work_app .placeholder {
    display: block;
    height: 100%;
    object-fit: cover;
    width: 100%;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .media {
    height: 100%;
    width: 100%;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .desktop_media {
    display: block;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .mobile_media {
    display: none;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .sound_toggle {
    align-items: center;
    background: rgb(0 0 0 / 0.45);
    border: 1px solid rgb(255 255 255 / 0.18);
    border-radius: 50%;
    color: #ffffff;
    cursor: pointer;
    display: flex;
    height: 36px;
    justify-content: center;
    padding: 0;
    position: absolute;
    right: 12px;
    top: 12px;
    transition:
      background 0.2s ease,
      border-color 0.2s ease;
    width: 36px;
    z-index: 2;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .sound_toggle:hover {
    background: rgb(0 0 0 / 0.62);
    border-color: var(--accent-color);
  }

  #shopify-section-{{ section.id }} .how_it_work_app .sound_toggle:focus-visible {
    outline: 2px solid var(--accent-color);
    outline-offset: 2px;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .sound_toggle .icon {
    display: block;
    height: 18px;
    width: 18px;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .sound_toggle .icon_unmuted {
    display: none;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .sound_toggle.is_unmuted .icon_muted {
    display: none;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .sound_toggle.is_unmuted .icon_unmuted {
    display: block;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .placeholder {
    background: linear-gradient(135deg, #51384b, #171117);
  }

  #shopify-section-{{ section.id }} .how_it_work_app .slide_content {
    margin: 30px auto 0;
    max-width: 650px;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .slide_title {
    color: #ffffff;
    font-size: 24px;
    font-weight: 600;
    line-height: 1.2;
    margin: 0;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .title_line {
    background: var(--accent-color);
    height: 2px;
    margin: 12px 0 14px;
    width: 76px;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .slide_description {
    color: #ffffff;
    font-size: 16px;
    line-height: 1.4;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .arrow {
    align-items: center;
    background: #3f3f42;
    border: 0;
    border-radius: 50%;
    color: var(--accent-color);
    cursor: pointer;
    display: flex;
    height: 44px;
    justify-content: center;
    padding: 0;
    position: absolute;
    top: 307px;
    transform: translateY(-50%);
    transition:
      background 0.2s ease,
      opacity 0.2s ease;
    width: 44px;
    z-index: 2;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .arrow:hover {
    background: #555558;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .arrow:focus-visible {
    outline: 2px solid var(--accent-color);
    outline-offset: 3px;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .arrow[disabled] {
    cursor: default;
    opacity: 0.35;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .arrow_prev {
    left: 0;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .arrow_next {
    right: 0;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .arrow_icon {
    border-right: 1.5px solid currentcolor;
    border-top: 1.5px solid currentcolor;
    display: block;
    height: 9px;
    width: 9px;
  }

  #shopify-section-{{ section.id }} .how_it_work_app .arrow_prev .arrow_icon {
    margin-left: 4px;
    transform: rotate(-135deg);
  }

  #shopify-section-{{ section.id }} .how_it_work_app .arrow_next .arrow_icon {
    margin-right: 4px;
    transform: rotate(45deg);
  }

  @media screen and (min-width: 990px) {
    #shopify-section-{{ section.id }} .how_it_work_app .carousel {
      max-width: 1200px;
      padding: 0 68px;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .viewport {
      border: 1px solid rgb(255 255 255 / 0.12);
      border-radius: 18px;
      box-shadow: 0 24px 70px rgb(0 0 0 / 0.42);
      max-width: 1064px;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .slide {
      display: grid;
      grid-template-columns: minmax(0, 44%) minmax(0, 56%);
      height: clamp(500px, 42vw, 580px);
    }

    #shopify-section-{{ section.id }} .how_it_work_app .image_wrap {
      aspect-ratio: auto;
      border-radius: 0;
      height: 100%;
      margin: 0;
      max-width: none;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .slide_content {
      background:
        radial-gradient(circle at 100% 0%, rgb(242 135 236 / 0.13), transparent 38%),
        linear-gradient(135deg, #21151f 0%, #111111 72%);
      display: flex;
      flex-direction: column;
      justify-content: center;
      margin: 0;
      max-width: none;
      padding: clamp(42px, 5vw, 76px);
      position: relative;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .slide_content::before {
      background: var(--accent-color);
      content: "";
      height: 64px;
      left: 0;
      position: absolute;
      top: clamp(42px, 5vw, 76px);
      width: 2px;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .slide_title {
      font-size: clamp(28px, 2.4vw, 38px);
      font-weight: 500;
      letter-spacing: -0.025em;
      line-height: 1.08;
      max-width: 460px;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .title_line {
      margin: 20px 0 22px;
      width: 96px;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .slide_description {
      color: rgb(255 255 255 / 0.82);
      font-size: 18px;
      line-height: 1.5;
      max-width: 480px;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .arrow {
      background: #29272a;
      border: 1px solid rgb(255 255 255 / 0.14);
      height: 48px;
      top: 50%;
      width: 48px;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .arrow:hover {
      background: var(--accent-color);
      color: #171117;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .arrow_prev {
      left: 8px;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .arrow_next {
      right: 8px;
    }
  }

  @media screen and (max-width: 749px) {
    #shopify-section-{{ section.id }} .how_it_work_app {
      padding: {{ section.settings.mobile_padding_top }}px 0 {{ section.settings.mobile_padding_bottom }}px;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .inner {
      padding: 0 14px;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .header {
      margin-bottom: 18px;
      max-width: 100%;
      padding: 0;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .eyebrow {
      font-size: 16px;
      margin-bottom: 13px;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .section_title {
      font-size: 27px;
      line-height: 1.02;
      max-width: 390px;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .section_description {
      font-size: 16px;
      line-height: 1.27;
      margin-top: 17px;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .viewport {
      max-width: none;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .desktop_media {
      display: none;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .mobile_media {
      display: block;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .image_wrap {
      border-radius: 10px;
      max-width: min(61.2vw, 260px);
    }

    #shopify-section-{{ section.id }} .how_it_work_app .sound_toggle {
      height: 28px;
      right: 8px;
      top: 8px;
      width: 28px;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .sound_toggle .icon {
      height: 14px;
      width: 14px;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .slide_content {
      margin-top: 31px;
      max-width: none;
      padding: 0 5px;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .slide_title {
      font-size: 18px;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .title_line {
      margin: 10px 0 13px;
      width: 76px;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .slide_description {
      font-size: 14px;
      line-height: 1.24;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .arrow {
      height: 28px;
      top: min(40.8vw, 173px);
      width: 28px;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .arrow_prev {
      left: 0;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .arrow_next {
      right: 0;
    }

    #shopify-section-{{ section.id }} .how_it_work_app .arrow_icon {
      height: 7px;
      width: 7px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    #shopify-section-{{ section.id }} .how_it_work_app .track {
      transition: none;
    }
  }
</style>

<section class="how_it_work_app" id="how-it-work-app-{{ section.id }}">
  <div class="inner">
    <div class="header">
      {%- if section.settings.subtitle != blank -%}
        <p class="eyebrow">{{ section.settings.subtitle | escape }}</p>
      {%- endif -%}

      {%- if section.settings.title != blank -%}
        <h2 class="section_title">{{ section.settings.title | escape }}</h2>
      {%- endif -%}

      {%- if section.settings.description != blank -%}
        <div class="section_description">{{ section.settings.description }}</div>
      {%- endif -%}
    </div>

    {%- if section.blocks.size > 0 -%}
      <div class="carousel">
        <div
          class="viewport"
          role="region"
          aria-roledescription="carousel"
          aria-label="{{ section.settings.title | default: 'SensOn App' | escape }}"
          tabindex="0"
        >
          <div class="track">
            {%- for block in section.blocks -%}
              <article
                class="slide"
                data-app-slide="{{ forloop.index0 }}"
                role="group"
                aria-roledescription="slide"
                aria-label="{{ forloop.index }} / {{ forloop.length }}"
                aria-hidden="{% if forloop.first %}false{% else %}true{% endif %}"
                {{ block.shopify_attributes }}
              >
                <div class="image_wrap">
                  {%- liquid
                    assign has_desktop_video = false
                    assign has_mobile_video = false

                    if block.settings.video != blank
                      assign has_desktop_video = true
                    endif

                    if block.settings.mobile_video != blank
                      assign has_mobile_video = true
                    endif
                  -%}

                  {%- if has_desktop_video and has_mobile_video -%}
                    <div class="media desktop_media">
                      {{
                        block.settings.video
                        | video_tag:
                          class: 'video',
                          autoplay: true,
                          loop: true,
                          muted: true,
                          playsinline: true,
                          controls: false,
                          preload: 'metadata',
                          image_size: '1200x'
                      }}
                    </div>
                    <div class="media mobile_media">
                      {{
                        block.settings.mobile_video
                        | video_tag:
                          class: 'video',
                          autoplay: true,
                          loop: true,
                          muted: true,
                          playsinline: true,
                          controls: false,
                          preload: 'metadata',
                          image_size: '800x'
                      }}
                    </div>
                  {%- elsif has_desktop_video -%}
                    <div class="media">
                      {{
                        block.settings.video
                        | video_tag:
                          class: 'video',
                          autoplay: true,
                          loop: true,
                          muted: true,
                          playsinline: true,
                          controls: false,
                          preload: 'metadata',
                          image_size: '1200x'
                      }}
                    </div>
                  {%- elsif has_mobile_video -%}
                    <div class="media">
                      {{
                        block.settings.mobile_video
                        | video_tag:
                          class: 'video',
                          autoplay: true,
                          loop: true,
                          muted: true,
                          playsinline: true,
                          controls: false,
                          preload: 'metadata',
                          image_size: '800x'
                      }}
                    </div>
                  {%- elsif block.settings.image != blank -%}
                    {{
                      block.settings.image
                      | image_url: width: 1200
                      | image_tag:
                        class: 'image',
                        loading: 'lazy',
                        sizes: '(max-width: 749px) 61vw, (min-width: 990px) 470px, 460px',
                        widths: '240, 320, 460, 640, 800, 1000, 1200',
                        alt: block.settings.title
                    }}
                  {%- else -%}
                    <div class="placeholder"></div>
                  {%- endif -%}

                  {%- if has_desktop_video or has_mobile_video -%}
                    <button
                      class="sound_toggle"
                      type="button"
                      data-sound-toggle
                      aria-label="打开声音"
                      aria-pressed="false"
                    >
                      <svg class="icon icon_muted" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                        <path
                          d="M11 5 6 9H3v6h3l5 4V5Z"
                          stroke="currentColor"
                          stroke-width="1.8"
                          stroke-linejoin="round"
                        />
                        <path
                          d="m16 9.5 5 5M21 9.5l-5 5"
                          stroke="currentColor"
                          stroke-width="1.8"
                          stroke-linecap="round"
                        />
                      </svg>
                      <svg class="icon icon_unmuted" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                        <path
                          d="M11 5 6 9H3v6h3l5 4V5Z"
                          stroke="currentColor"
                          stroke-width="1.8"
                          stroke-linejoin="round"
                        />
                        <path
                          d="M15.5 8.5a5 5 0 0 1 0 7M18.5 6a8.5 8.5 0 0 1 0 12"
                          stroke="currentColor"
                          stroke-width="1.8"
                          stroke-linecap="round"
                        />
                      </svg>
                    </button>
                  {%- endif -%}
                </div>

                <div class="slide_content">
                  {%- if block.settings.title != blank -%}
                    <h3 class="slide_title">{{ block.settings.title | escape }}</h3>
                  {%- endif -%}

                  <div class="title_line"></div>

                  {%- if block.settings.description != blank -%}
                    <div class="slide_description">{{ block.settings.description }}</div>
                  {%- endif -%}
                </div>
              </article>
            {%- endfor -%}
          </div>
        </div>

        <button class="arrow arrow_prev" type="button" aria-label="上一张">
          <span class="arrow_icon" aria-hidden="true"></span>
        </button>
        <button class="arrow arrow_next" type="button" aria-label="下一张">
          <span class="arrow_icon" aria-hidden="true"></span>
        </button>
      </div>
    {%- endif -%}
  </div>
</section>

<script>
  (() => {
    const section = document.querySelector("#how-it-work-app-{{ section.id }}");

    if (!section || section.dataset.carouselInitialized === "true") {
      return;
    }

    section.dataset.carouselInitialized = "true";

    const track = section.querySelector(".track");
    const viewport = section.querySelector(".viewport");
    const slides = Array.from(section.querySelectorAll("[data-app-slide]"));
    const previousButton = section.querySelector(".arrow_prev");
    const nextButton = section.querySelector(".arrow_next");

    if (!track || !viewport || slides.length === 0) {
      return;
    }

    let activeIndex = 0;
    let touchStartX = 0;
    let isMuted = true;

    const soundToggleButtons = Array.from(section.querySelectorAll("[data-sound-toggle]"));
    const videos = Array.from(section.querySelectorAll("video"));

    const applyMuteState = () => {
      videos.forEach((video) => {
        video.muted = isMuted;
      });

      soundToggleButtons.forEach((button) => {
        button.classList.toggle("is_unmuted", !isMuted);
        button.setAttribute("aria-pressed", isMuted ? "false" : "true");
        button.setAttribute("aria-label", isMuted ? "打开声音" : "关闭声音");
      });
    };

    const syncSlideVideos = () => {
      slides.forEach((slide, slideIndex) => {
        const slideVideos = slide.querySelectorAll("video");
        const isActive = slideIndex === activeIndex;

        slideVideos.forEach((video) => {
          const media = video.closest(".media");
          const isVisible = !media || window.getComputedStyle(media).display !== "none";

          video.muted = isMuted;

          if (isActive && isVisible) {
            const playPromise = video.play();

            if (playPromise && typeof playPromise.catch === "function") {
              playPromise.catch(() => {});
            }

            return;
          }

          video.pause();

          if (!isActive) {
            video.currentTime = 0;
          }
        });
      });
    };

    const showSlide = (index) => {
      activeIndex = (index + slides.length) % slides.length;
      track.style.transform = `translate3d(-${activeIndex * 100}%, 0, 0)`;

      slides.forEach((slide, slideIndex) => {
        slide.setAttribute("aria-hidden", slideIndex === activeIndex ? "false" : "true");
      });

      syncSlideVideos();
    };

    const hasMultipleSlides = slides.length > 1;
    previousButton.disabled = !hasMultipleSlides;
    nextButton.disabled = !hasMultipleSlides;

    previousButton.addEventListener("click", () => {
      showSlide(activeIndex - 1);
    });

    nextButton.addEventListener("click", () => {
      showSlide(activeIndex + 1);
    });

    soundToggleButtons.forEach((button) => {
      button.addEventListener("click", (event) => {
        event.stopPropagation();
        isMuted = !isMuted;
        applyMuteState();
        syncSlideVideos();
      });
    });

    viewport.addEventListener("keydown", (event) => {
      if (event.key === "ArrowLeft") {
        showSlide(activeIndex - 1);
      }

      if (event.key === "ArrowRight") {
        showSlide(activeIndex + 1);
      }
    });

    viewport.addEventListener(
      "touchstart",
      (event) => {
        touchStartX = event.touches[0].clientX;
      },
      { passive: true }
    );

    viewport.addEventListener(
      "touchend",
      (event) => {
        const touchDistance = event.changedTouches[0].clientX - touchStartX;

        if (Math.abs(touchDistance) < 45) {
          return;
        }

        showSlide(touchDistance > 0 ? activeIndex - 1 : activeIndex + 1);
      },
      { passive: true }
    );

    if (window.Shopify && window.Shopify.designMode) {
      document.addEventListener("shopify:block:select", (event) => {
        const selectedSlide = event.target.closest("[data-app-slide]");

        if (selectedSlide && section.contains(selectedSlide)) {
          showSlide(Number(selectedSlide.dataset.appSlide));
        }
      });
    }

    applyMuteState();
    showSlide(0);

    window.addEventListener("resize", syncSlideVideos);
  })();
</script>

{% schema %}
{
  "name": "How it works app",
  "tag": "section",
  "class": "shopify-section--how-it-works-app",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "text",
      "id": "subtitle",
      "label": "副标题",
      "default": "SensOn App"
    },
    {
      "type": "text",
      "id": "title",
      "label": "主标题",
      "default": "What You Can Do in the SensOn App"
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "描述",
      "default": "<p>The SensOn App is an AI intimacy app where AI fantasy, interactive audio, and device control come together.</p>"
    },
    {
      "type": "color",
      "id": "accent_color",
      "label": "强调色",
      "default": "#f287ec"
    },
    {
      "type": "header",
      "content": "模块间距"
    },
    {
      "type": "range",
      "id": "desktop_padding_top",
      "label": "PC 端顶部间距",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 72
    },
    {
      "type": "range",
      "id": "desktop_padding_bottom",
      "label": "PC 端底部间距",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 80
    },
    {
      "type": "range",
      "id": "mobile_padding_top",
      "label": "移动端顶部间距",
      "min": 0,
      "max": 120,
      "step": 2,
      "unit": "px",
      "default": 42
    },
    {
      "type": "range",
      "id": "mobile_padding_bottom",
      "label": "移动端底部间距",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 48
    }
  ],
  "blocks": [
    {
      "type": "app_feature",
      "name": "App 功能",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "图片",
          "info": "未上传视频时显示。建议使用 3:4 的竖版图片。"
        },
        {
          "type": "video",
          "id": "video",
          "label": "PC 端视频",
          "info": "优先于图片显示。若未上传移动端视频，移动端也会使用此视频。"
        },
        {
          "type": "video",
          "id": "mobile_video",
          "label": "移动端视频",
          "info": "可选。未上传时移动端回退使用 PC 端视频。"
        },
        {
          "type": "text",
          "id": "title",
          "label": "标题",
          "default": "Choose Interactive Audio"
        },
        {
          "type": "richtext",
          "id": "description",
          "label": "描述",
          "default": "<p>Explore interactive audio stories created with women in mind.</p>"
        }
      ]
    }
  ],
  "max_blocks": 8,
  "presets": [
    {
      "name": "How it works app",
      "category": "Content",
      "blocks": [
        {
          "type": "app_feature"
        },
        {
          "type": "app_feature",
          "settings": {
            "title": "Explore AI Fantasy",
            "description": "<p>Discover immersive AI characters and experiences designed around your preferences.</p>"
          }
        },
        {
          "type": "app_feature",
          "settings": {
            "title": "Control Your Device",
            "description": "<p>Connect SensOn S1 and adjust your experience directly from the app.</p>"
          }
        }
      ]
    }
  ]
}
{% endschema %}
