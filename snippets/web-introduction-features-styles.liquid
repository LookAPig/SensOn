<style>
  #shopify-section-{{ section.id }} .web_intro_features {
    background: {{ section.settings.background_color }};
    box-sizing: border-box;
    width: 100%;
  }

  #shopify-section-{{ section.id }} .web_intro_features * {
    box-sizing: border-box;
  }

  #shopify-section-{{ section.id }} .web_intro_features .features_scroll {
    width: 100%;
  }

  #shopify-section-{{ section.id }} .web_intro_features .features_track {
    align-items: center;
    display: flex;
    justify-content: space-evenly;
    margin: 0 auto;
    max-width: 1440px;
    padding: {{ section.settings.padding_desktop }}px 48px;
    width: 100%;
  }

  #shopify-section-{{ section.id }} .web_intro_features .feature_item {
    flex: 1 1 0;
    min-width: 0;
    text-align: center;
  }

  #shopify-section-{{ section.id }} .web_intro_features .feature_title {
    color: {{ section.settings.title_color }};
    font-size: 20px;
    font-weight: 700;
    line-height: 1.2;
    margin: 0;
  }

  #shopify-section-{{ section.id }} .web_intro_features .feature_subtitle {
    color: {{ section.settings.subtitle_color }};
    font-size: 14px;
    font-weight: 400;
    line-height: 1.3;
    margin: 4px 0 0;
  }

  #shopify-section-{{ section.id }} .web_intro_features .features_edge {
    display: none;
  }
</style>
